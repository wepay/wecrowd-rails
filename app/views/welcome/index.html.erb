<div data-step="1" data-intro="Click on any of the featured campaigns to make a donation. <br /> If you are trying to donate to a campaign you created, use the link from your campaign detail page..">
</div>
<div class="row text-center">
	<% @campaigns.each do |c| %>
  <div class="col-md-3 col-sm-6 main-content-feature">
    <a href="/campaign/<%= c.id %>">
    <div class="thumbnail">
      <img src="<%= asset_path c.image %>" alt="">
      <div class="caption">
        <h3><%= c.name %></h3>
        <p>Palo Alto, CA</p>
      </div>

      <div class="absolute-bottom">
        <div class="thumbnail-footer">
          <h1 class="inline-block">
            <%= c.goal.currency.symbol %><%= c.goal %>
          </h1>
          <h5 class="inline-block">
            <%= c.goal.currency %>
          </h5>

          <div class="campaign-progress-bar bg-gray">
            <div class="bg-orange full-height" style="width: <%= c.percent_complete %>%"></div>
          </div>

          <p class="inline-block" id="percent">
            <%= c.percent_complete %>%
          </p>
          <p class="inline-block right">
            10 days left
          </p>
        </div>
      </div>
    </div>
    </a>
  </div>
	<% end %>
</div>

<div class="demo">
  <div class="demo title">
    Experience Seamless White Label Payments with WePay Clear
  </div>
  <div class="demo content">
    <p class="p-modal">This site illustrates how a typical platform can use WePay Clear to create a seamless, partner-branded payments experience.</p>
    <p class="p-modal">Click the <b>Start Buyer's Demo</b> button to experience the payment flow for someone donating to a crowdfunding campaign.</p>
    <p class="p-modal">Click the <b>Start Seller's Demo</b> button to walk through the steps in setting up a new campaign, from creating the campaign page, to hooking up a bank account, to receiving donations</p>
  </div>
</div>