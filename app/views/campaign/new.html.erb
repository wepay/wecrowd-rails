<div class="user">

  <div id="spacer"></div>

  <div class="row">
    <div class="col-lg-12 main-content-feature">
      <%= form_tag("/campaign/new", 'data-persist' => 'garlic', 'data-destroy' => 'false' ) do %>

      <div class="col-lg-5 main-content-feature">
        <div class="row">
          <h3 class="upper">Campaign Details</h3>
        </div>

        <div class="row main-content-feature">
          <label for="email">Campaign Name</label>
          <%= text_field_tag(:campaign_name, params[:campaign_name], :class => 'text form-control input-lg gray', :placeholder => 'Coffee for Everyone!', :maxlength => 255) %>
        </div>

        <div class="row main-content-feature">
          <label for="email">Campaign Description</label>
          <%= text_field_tag(:campaign_description, params[:campaign_description], :class => 'text form-control input-lg gray', :placeholder => 'Let\'s supply coffee to the overworked people.') %>
        </div>

        <div class="row main-content-feature">
          <label for="email">Donation Goal</label>
          <%= text_field_tag(:campaign_goal, params[:campaign_goal], :class => 'text form-control input-lg gray', :placeholder => '$5,000', :maxlength => 10) %>
        </div>

        <div class="row">
          <label for="email">Account Type</label>
          <%= select_tag(:account_type, options_for_select({ "Individual" => "personal", "Business" =>"business", "Non-profit" => "nonprofit"}, params[:account_type]), :class => 'text form-control input-lg gray') %>
        </div>
      </div>
      <div class="col-lg-5 col-sm-offset-2 main-content-feature">
        <% if !signed_in? %>

          <div class="row">
            <h3 class="upper">User Details</h3>
          </div>

          <div class="row main-content-feature">
            <label for="name">Your first and last name<b>*</b></label>
            <%= text_field_tag(:user_name, params[:name], :class => 'text form-control input-lg gray', :placeholder => 'First & Last Name', :maxlength => 255) %>
          </div>

          <div class="row main-content-feature">
            <label for="email">Your email address<b>*</b></label>
            <%= text_field_tag(:user_email, params[:email], :class => 'text form-control input-lg gray', :placeholder => 'jane.doe@gmail.com', :maxlength => 255) %>
          </div>


          <div class="row main-content-feature">
            <label for="password">Password</label>
            <%= password_field_tag(:user_password, nil, :class => 'password form-control input-lg gray', :maxlength => 255) %>
          </div>
          
          <div class="row main-content-feature">
            <label for="accept_terms"><%= check_box_tag(:accept_terms, true, true) %>
              I accept the WeCrowd <a target="_blank" href="/welcome/terms">Terms of Service</a>.
            </label>
          </div>

            <div class="row main-content-feature">
              <label for="enable_mfa"><%= check_box_tag(:enable_mfa, true, true) %>
                Enable <a target="_blank" href="/mfa/about">MFA</a>.
              </label>
            </div>

        <% end %>
      </div>

      <div class="row">
        <div class="col-md-3 col-center">
          <button type="submit" class="btn btn-orange">Create</button>
        </div>
      </div>

      <% end %>
    </div>
  </div>
</div>
<div class="demo">
  <div class="demo title">Sign Up / Set Up</div>
  <div class="demo content">
    <p class="p-modal"><em>Logout if needed and go to Create Campaign</em></p>

    <p class="p-modal">This is an example of a typical campaign setup page for a crowdfunding site powered by WePay Clear. Pages like this can be completely customized to the needs of the site.</p>

    <p class="p-modal">For this demo, we've pre-filled the fields with example information. Please use a real email address as you will need to confirm your email to start the experience.</p>
 
    <p class="p-modal"><b>Click Create</b> to continue the demo.</p>
  </div>
</div>